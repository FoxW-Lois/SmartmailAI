<Page
	x:Class="SmartmailAI.Views.Pages.Login_Page"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behaviors="using:SmartmailAI.Behaviors"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:viewmodels="using:SmartmailAI.ViewModels.Pages"
	behaviors:NavigationViewHeaderBehavior.HeaderMode="Never"
	d:DataContext="{d:DesignInstance Type=viewmodels:Login_ViewModel}"
	NavigationCacheMode="Required"
	mc:Ignorable="d">

	<Grid
		x:Name="ContentArea"
		Width="360"
		HorizontalAlignment="Center"
		VerticalAlignment="Center">
		<StackPanel Spacing="16">
			<TextBlock x:Uid="Account_SignIn" Style="{ThemeResource TitleTextBlockStyle}" />
			<TextBox x:Uid="Account_Login" Text="{x:Bind ViewModel.Login, Mode=TwoWay}" />
			<PasswordBox x:Name="PasswordBox" x:Uid="Account_Password" />

			<TextBlock
				Foreground="Red"
				Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
				TextWrapping="Wrap"
				Visibility="{Binding ErrorVisibility}" />

			<Button x:Uid="Account_LogOn" Click="OnLoginClicked" />
			<HyperlinkButton x:Uid="Account_Create" Click="OnRegisterClicked" />
		</StackPanel>
	</Grid>
</Page>
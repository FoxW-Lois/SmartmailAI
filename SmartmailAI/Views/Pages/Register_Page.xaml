<Page
	x:Class="SmartmailAI.Views.Pages.Register_Page"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behaviors="using:SmartmailAI.Behaviors"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:viewmodels="using:SmartmailAI.ViewModels.Pages"
	behaviors:NavigationViewHeaderBehavior.HeaderMode="Never"
	d:DataContext="{d:DesignInstance Type=viewmodels:Register_ViewModel}"
	NavigationCacheMode="Required"
	mc:Ignorable="d">

	<Grid
		x:Name="ContentArea"
		Width="360"
		HorizontalAlignment="Center"
		VerticalAlignment="Center">
		<StackPanel Spacing="16">
			<TextBlock x:Uid="Account_SignUp" Style="{ThemeResource TitleTextBlockStyle}" />
			<TextBox
				x:Name="LoginBox"
				x:Uid="Account_Login"
				Text="{x:Bind ViewModel.Login, Mode=TwoWay}" />
			<TextBox
				x:Name="PhoneNumberBox"
				x:Uid="Account_Cellphone_Number"
				Text="{x:Bind ViewModel.PhoneNumber, Mode=TwoWay}" />
			<PasswordBox x:Name="PasswordBox" x:Uid="Account_Password" />
			<PasswordBox x:Name="ConfirmPasswordBox" x:Uid="Account_ConfirmPassword" />

			<TextBlock
				Foreground="Red"
				Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
				TextWrapping="Wrap"
				Visibility="{Binding ErrorVisibility}" />

			<Button x:Uid="Account_Create" Click="OnRegisterClicked" />
			<HyperlinkButton x:Uid="Account_Alreadyhave" Click="OnBackToLoginClicked" />
		</StackPanel>
	</Grid>
</Page>